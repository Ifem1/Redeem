# Environment

Canonical target: GenLayer Studionet, chain ID 61999, currency GEN. Keep secrets in `.env`; never commit them. The frontend must use an external wallet and must not generate or store private keys.

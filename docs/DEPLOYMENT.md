# Deployment

Target: GenLayer Studionet, chain ID 61999. Deploy from the final green commit with REDEEM_ISSUER supplied only in the process environment. Record address, deployment transaction, commit SHA, runner hash, schema digest, and UTC timestamp. Vercel uses VITE_REDEEM_CONTRACT_ADDRESS set to that final address.

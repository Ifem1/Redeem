networks:
  default: studionet
  studionet:
    # Pre-configured by gltest; no private keys are committed.
    leader_only: false
paths:
  contracts: contracts
  artifacts: artifacts
environment: .env
